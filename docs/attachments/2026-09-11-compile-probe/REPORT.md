---
type: report
updated: 2026-09-20
---

# Compile probe, 2026-09-11

Object compile of GrafX2 `f84cb09dc59d706d6e7b28778b01ba911f52298c` for
AROS x86_64, both ABIs. **No link. No QEMU run.**

Per-file results are in [summary.json](summary.json).

## What was compiled

Flags (explicitly a probe, not the upstream Makefile):
`-std=gnu99 -O0 -Wall -Wno-pointer-sign -DUSE_SDL2 -DNOTTF=1 -DNORECOIL
-D__no_tifflib__` plus SDK include paths (`include` and `include/SDL2`).
File list from `OBJS` in `src/Makefile` with `$(APIOBJ)` = `sdlscreen.o`.

| Probe | Compiler | SDK | Units OK | Failures |
|---|---|---|---|---|
| ABIv1 | GCC 10.5.0, mainline build | mainline `Developer/` | 64/71 | 6 files missing `SDL_image.h`; `c64load.c` missing `6502.h` |
| ABIv11 | GCC 10.5.0, ABIv11 build | AROS One 1.3 SDK | 70/71 | `c64load.c` missing `6502.h` |

`version.c` is generated by the Makefile. `6502.c` / `6502.h` come from
redcode/6502 v0.1 via `3rdparty/Makefile`. Toolchains and SDKs were not mixed.

## What this did not show

Linking, SDL2_image at link time, renderer selection, mouse coordinates,
data-file loading, PNG/IFF I/O at runtime, or behaviour on any guest.
